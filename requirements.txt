# Core GUI
PySide6>=6.6

# Speech-to-text (offline Whisper via CTranslate2)
faster-whisper>=0.10.0

# Audio recording
sounddevice>=0.4.7
soundfile>=0.12.1
numpy>=1.23.0

# Efficient approximate nearest-neighbor search for embedding retrieval
hnswlib>=0.8

# Medical dictionary fuzzy matching
rapidfuzz>=3.0.0

# Offline English dictionary — the non-word guard for the spelling corrector.
# Bundles its own frequency dict (no network). When absent, the corrector
# degrades to a conservative ratio-only mode rather than risk over-correction.
pyspellchecker>=0.8

# Fast fuzzy-correction index (SymSpell) for the medical dictionary: a
# deletion-dictionary over the ~98k-term wordlist keeps "nearest known term
# within edit distance N" lookups sub-millisecond even with a large candidate
# pool. When absent, the corrector falls back to the slower rapidfuzz ratio path.
# editdistpy is symspellpy's fast (C) distance backend — since symspellpy 6.10 it
# is an OPTIONAL extra, and without it get_symspell()'s lookups raise ImportError
# and silently drop to the ~200x slower rapidfuzz 98k-term scan (which also fails
# to correct one-letter medical typos). Pin it explicitly so a fresh install gets
# the fast, correct path by default.
symspellpy>=6.7.7
editdistpy>=0.1.3

# Data-driven correction rules (corrections.yaml / user_corrections.yaml)
PyYAML>=6.0

# Word document export
python-docx>=1.1.0

# Web app
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
jinja2>=3.1.2
httpx>=0.24.0

# Cloud training (Lightning AI) — local side only.
# The API key is stored in the OS keychain via keyring; httpx (above) is the
# REST transport. Model fine-tuning itself runs on Lightning AI using the
# heavier deps listed in scripts/lightning/requirements_cloud.txt.
keyring>=24.0
