# Core GUI
PySide6>=6.6

# Speech-to-text (offline Whisper via CTranslate2)
faster-whisper>=0.10.0

# Audio recording
sounddevice>=0.4.7
soundfile>=0.12.1
numpy>=1.23.0

# Medical dictionary fuzzy matching
rapidfuzz>=3.0.0

# Word document export (optional but recommended)
python-docx>=1.1.0
